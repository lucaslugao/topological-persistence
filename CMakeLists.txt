CMAKE_MINIMUM_REQUIRED(VERSION 2.8)
PROJECT(TD5)

set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -std=c++11  -O3") # -ggdb

ADD_EXECUTABLE(TD5 src/main.cpp)

add_custom_target(run
    COMMAND TD5
    DEPENDS TD5
    WORKING_DIRECTORY ${CMAKE_PROJECT_DIR}
)